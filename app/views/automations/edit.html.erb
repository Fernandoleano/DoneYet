<div class="max-w-7xl mx-auto">
  <h1 class="text-3xl font-bold text-white mb-8">Edit Automation</h1>

  <%= form_with model: @automation, class: "space-y-6", data: { controller: "automation-builder" } do |f| %>
    <%= f.hidden_field :config, value: @automation.config&.to_json || '{}', data: { automation_builder_target: "configField" } %>
    
    <div class="bg-gray-800 border border-gray-700 rounded-2xl p-6">
      <%= f.label :name, "Automation Name", class: "block text-sm font-medium text-gray-300 mb-2" %>
      <%= f.text_field :name, class: "w-full bg-gray-900 border border-gray-700 rounded-lg px-4 py-3 text-white placeholder-gray-500 focus:ring-2 focus:ring-indigo-500" %>
    </div>

    <div class="bg-gray-800 border border-gray-700 rounded-2xl p-6">
      <%= f.label :automation_type, "Automation Type", class: "block text-sm font-medium text-gray-300 mb-2" %>
      <%= f.select :automation_type, 
        options_for_select([
          ["Recurring Meeting", "recurring_meeting"],
          ["Reminder Schedule", "reminder_schedule"],
          ["Integration Trigger", "integration_trigger"]
        ], @automation.automation_type),
        {},
        class: "w-full bg-gray-900 border border-gray-700 rounded-lg px-4 py-3 text-white focus:ring-2 focus:ring-indigo-500"
      %>
    </div>

    <div class="bg-gray-800 border border-gray-700 rounded-2xl p-6">
      <p class="text-sm text-gray-400 mb-2">Current Configuration:</p>
      <pre class="bg-gray-900 border border-gray-700 rounded-lg p-4 text-xs text-gray-300 overflow-auto"><%= JSON.pretty_generate(@automation.config || {}) %></pre>
    </div>

    <div class="flex gap-4">
      <%= f.submit "Update Automation", class: "flex-1 px-6 py-3 bg-indigo-600 hover:bg-indigo-500 text-white font-semibold rounded-lg transition" %>
      <%= link_to "Cancel", automations_path, class: "px-6 py-3 bg-gray-700 hover:bg-gray-600 text-white font-semibold rounded-lg transition" %>
    </div>
  <% end %>
</div>
